# Electron-friendly pnpm layout. electron-vite + electron-builder expect a
# flat node_modules tree to locate the prebuilt binary and rebuild native
# modules; pnpm's default isolated layout hides them under .pnpm/.
node-linker=hoisted
